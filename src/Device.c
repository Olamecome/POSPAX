#include "xDefs.h"
#include "file_handler.h"

#define DEVICE_STORE "device.params"

//
//int readDevice(Device* device) {
//	memset(device, 0, sizeof(Device));
//
//	return loadFileData(DEVICE_STORE, device, sizeof(Device));
//}
//
//int saveDevice(Device* device) {
//	return saveFileData(DEVICE_STORE, device, sizeof(Device));
//}